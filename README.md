sudo apt-get install libtool automake autoconf nasm git nodejs npm
git clone https://github.com/flygy2003/React-Boilerplate
git checkout offline
npm install
npm install rpi-gpio --save-dev
sudo node onnline/offline.js
